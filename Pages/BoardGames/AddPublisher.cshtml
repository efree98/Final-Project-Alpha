@page
@model AddBoardGameModel

@{
    ViewData["Title"] = "Add Course";
}

<h1>Create</h1>

<h4>Course</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="BoardGamePublisher.PublisherId" class="control-label"></label>
                <select asp-for="BoardGamePublisher.PublisherId" asp-items="Model.BoardGameDropDown" class="form-control">
                    <option value="">--Select Game--</option> 
                </select>
                <span asp-validation-for="BoardGamePublisher.PublisherId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BoardGamePublisher.Publishing_Company" class="control-label"></label>
                <input asp-for="BoardGamePublisher.Publishing_Company " class="form-control" />
                <span asp-validation-for="BoardGamePublisher.Publishing_Company" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Add Course" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}